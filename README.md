# 搜题候选三阶段过滤器（面试实操任务）

25 分钟实操任务：修复一份 AI 生成的初版实现。领域知识不需要先验，全部在 `TASK.md` 里。

## 快速开始

```bash
python3 run_pipeline.py    # 初始状态跑不通是预期的——这就是任务的一部分
```

## 文件

| 文件 | 说明 |
|---|---|
| `TASK.md` | 任务说明：背景、数据说明、规格、产出要求（**先读这个**） |
| `recall_filter.py` | 待修的初版实现（三个阶段函数） |
| `run_pipeline.py` | 入口脚本，对 mock 数据跑全链路 |
| `mock_input.json` | 2 个 query 的模拟召回结果 |
