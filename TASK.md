# 面试任务：搜题候选三阶段过滤器（25 分钟）

## 背景（领域知识都在这，不需要先验）

我们的拍照搜题链路里有一条捷径：用户拍照搜题时，先查一个历史高质题库（库 id 为 `8889` / `8899`），召回的候选经过**三个阶段**的处理后输出最终结果：

1. **阶段一（召回过滤）**：按质量规则过滤召回候选
2. **阶段二（粗排截断）**：粗排打分后全局取 topN，控制进精排的量
3. **阶段三（精排后路由）**：只保留「相同题」通道的候选，按精排分数取 topK

## 现状

目录里有一份**AI 生成的初版实现** `recall_filter.py`，附 mock 数据 `mock_input.json` 和入口脚本 `run_pipeline.py`。提交者声称"能跑、符合规格"。**不要轻信。**

## 你的任务

1. 让 pipeline 跑通：`python3 run_pipeline.py`
2. 对照下面的规格，核对实现是否一致，**修复所有与规格不符的地方**
3. 用你认为可靠的方式**证明**修对了（写测试、手算对拍、逐条核对输出均可，材料请保留）
4. 产出：修好的 `recall_filter.py`、重新生成的 `output.json`、你的验证材料。**保持 `recall_filter.py` 文件名与 `filter_recall_stage` / `truncate_prerank` / `filter_postrank` 三个函数名不变**（入口按名导入；文件内部随便重构）

时间 25 分钟，做什么取舍自己决定。可以用任何 AI 工具。

## 数据说明

`mock_input.json` 包含 2 个 query，每个 query 的 `query_res` 是 `{库id: [候选, ...]}`。为简化练习，**粗排分数已预置在候选的 `pre_ranking_score` 字段里**（真实链路中它由粗排模型产生）。**数据是从线上采样脱敏的——和任何真实线上数据一样，别假设它完全干净。**候选项字段：


| 字段                  | 含义                                              |
| ------------------- | ----------------------------------------------- |
| `score`             | 召回向量相似度                                         |
| `is_same`           | 历史同题复用标注（int，可能为 0/1/2）                         |
| `api_id`            | 召回接口 id（**可能是 int 也可能是 str**）                   |
| `pre_ranking_score` | 粗排分数（预置）                                        |
| `is_same_vl`        | 精排通道判定，取值 `"相同"` / `"相似"` / 可能缺失                |
| `ranker_score`      | 精排分数，**两种格式并存：`0.88` 或 `[[0.88]]`**，也可能为 `null` |


## 规格（以此为准）

### 阶段一 `filter_recall_stage`

- 只对目标库 `8889` / `8899` 生效；**其他库原样透传，不做任何过滤**
- 目标库中，一个候选必须**同时**满足以下条件才保留：
  - `score >= 0.90`（**含边界**，0.90 保留）
  - `is_same != 1`（历史同题复用 bad 率 64%，过滤；其他值保留）
  - `api_id` 不在 `{777}` 中（旧接口 bad 率 77%，过滤；注意 int/str 都要拦住）
- 过滤后每个目标库按 `score` 降序保留最多 20 条

### 阶段二 `truncate_prerank`

- **全局展平所有库**的候选，按 `pre_ranking_score` 降序，取 topN 后重组为 `{库id: [...]}`
- 本练习中 N=5（入口脚本已传参）

### 阶段三 `filter_postrank`

- 只保留 `is_same_vl == "相同"` 的候选（"相似"通道 bad 率 88.5%，整条通道丢弃，**无论分数多高**）
- 按 `ranker_score` 降序取 topK（本练习 K=5）；`ranker_score` 缺失/无法解析视为 -inf 排末位
- **没有任何"相同"候选时返回 `[]`**（表示 fallback 走解题链路）
- `question_type` / `thresholds` 是**预留参数，本次不实现**。产品侧后续计划按下表按题型加阈值过滤（仅供了解，**不要写进代码**）：
  
  | 题型  | 阈值               |
  | --- | ---------------- |
  | 填空题 | 4.1              |
  | 选择题 | 4.0              |
  | 解答题 | 4.3              |
  | 判断题 | 不启用（强制 fallback） |
  

## 提示

- 先想清楚"怎么算对"，再动手；最后我们要听你讲"你怎么证明它对了"
- `output.json` 里每条结果的来源和顺序，我们都可能追问

